export enum QRCodeTypeEnum {
  book = `book`,
  contact = `contact`,
  email = `email`,
  link = `link`,
  phone = `phone`,
  sms = `sms`,
  text = `text`,
  wifi = `wifi`,
}

export enum NetworkTypeEnum {
  open = `open`,
  wep = `wep`,
  wpa_wpa2 = `wpa/wpa2`,
}
