enum QRCodeTypeEnum {
  wifi = `wifi`,
  link = `link`,
  text = `text`,
  phone = `phone`,
  contact = `contact`,
  sms = `sms`,
  book = `book`,
  email = `email`,
  custom = `custom`,
}

export default QRCodeTypeEnum;
